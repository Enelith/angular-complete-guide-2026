import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: true, // With Angular 20+, this property is set to TRUE by default (so in our case, it's useless)
  templateUrl: './header.html'
})
export class HeaderComponent {
  // TODO: How do we use it now?

}
