# 02-30 We Need More Flexible Components!

For the purpose of this demo, this component won't be used (the `user` component will be used instead with the Signals mechanism 
to show what Signals can do ~ the rest of the app will use the normal way to deal with changes)
